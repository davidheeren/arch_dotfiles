#!/bin/bash

# wofi entries eat into the right border. not sure why theres this much padding

clip=$(cliphist -preview-width 80 list | "$HOME/bin/process-toggle" wofi -S dmenu -p clip)

if [[ -n "$clip" ]]; then
    echo "$clip" | cliphist decode | wl-copy
fi
