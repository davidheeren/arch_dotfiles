profile {
  time = 00:00   # This ensures the profile is activated at the start of the day
  temperature = 4000 # Your desired color temperature (Kelvin) for all times
  # gamma = 95    # Optional: Your desired gamma correction (uncomment to enable)
}
